package frc.robot;

import frc.robot.subsystems.DriveTrain;

/**
 * This is a class that contains all subsystems.
 * 
 * Using this allows us to create commands on the fly (to be used with the autonomous JSON).
 */
public class SubsystemContainer {
    public DriveTrain driveTrain;
}